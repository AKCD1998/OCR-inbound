# Phase 0 Evidence

Date: 2026-07-22

## Repository and governance

- Worktree baseline is recorded in `docs/IMPLEMENTATION_STATUS.md`.
- Search found no local database/migration/five-schema implementation outside documentation.
  This is `NOT_FOUND_IN_WORKSPACE`, not evidence that no external schema exists.
- Owner authorization is recorded in Project Bible §9.1 and ledger §17. Current stage and first-five
  priority were not changed and no milestone completion is claimed.

## OCR contract

The existing `ocr_feasibility.py` remains unchanged. The versioned adapter must consume its additive
artifact contract, including `analysis.json`, page images, raw engine outputs, fusion evidence,
`tables/table_cells.json`, and Layer E `tables/table_line_items.json` plus its review gate. The
existing pipeline command remains the only real Layers A–E invocation path.

## ADA visual evidence only

`adapos_receiving_probe.png` visually supports only the version family `4.6006.x`, display
`SERVER\\sqlexpress/AdaAcc`, a product grid, `PRBCHYY-######`, Save/Approve/Cancel controls, VAT 7%,
THB, not-approved status, and recorder text `dao1`.

It does not prove window/control classes, control count, Win32 set/read reliability, AdaAcc schema or
permissions, grid keyboard behavior, popup handling, button semantics, document-number mapping, or
whether `dao1` is shared. No live action was attempted.

## Toolchain snapshot

- `.venv`: Python 3.11.9; Win32/ODBC/Pillow available.
- PySide6 and pytest-qt unavailable.
- .NET SDK unavailable.
- Node/npm available.
- Live ADA/AdaAcc flags must remain disabled.

## Comparative UI spike

`spikes/ui_comparison/EVIDENCE.md` records the shared 120-row workload and exact toolchain results.
Local-web was selected in ADR-002; PySide6 is unavailable and .NET SDK is unavailable. Chrome
headless capture returned failure, although a delayed 125% artifact was found and manually inspected
for Thai/evidence/focus rendering. No 100/150% visual pass or reliable capture claim is made.
